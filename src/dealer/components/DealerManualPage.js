import React from 'react';
import DealerManual from '../../components/tours/DealerManual';

/**
 * DealerManualPage component renders the dealer manual within the dealer layout.
 * 
 * @component
 * @returns {JSX.Element} Dealer manual page
 */
const DealerManualPage = () => {
  return <DealerManual />;
};

export default DealerManualPage;