import React from 'react';
import AdminManual from '../../components/tours/AdminManual';

/**
 * AdminManualPage component renders the admin manual within the admin layout.
 * 
 * @component
 * @returns {JSX.Element} Admin manual page
 */
const AdminManualPage = () => {
  return <AdminManual />;
};

export default AdminManualPage;